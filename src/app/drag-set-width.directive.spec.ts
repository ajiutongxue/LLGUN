import { DragSetWidthDirective } from './drag-set-width.directive';

describe('DragSetWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new DragSetWidthDirective();
    expect(directive).toBeTruthy();
  });
});

import { PaintFrameDirective } from './paint-frame.directive';

describe('PaintFrameDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintFrameDirective();
    expect(directive).toBeTruthy();
  });
});
